defn nat : atom
   | z = nat
   | s = nat -> nat

defn odd : nat -> atom
   | odd/one = odd (s z)
   | odd/n = [A] even A -> odd (s A)

defn even : nat -> atom
   | even/zero = even z
   | even/succ = [B] odd B -> even (s B)

